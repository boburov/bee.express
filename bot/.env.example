# Telegram
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=bee_express_bot

# Mini App URL (HTTPS in prod; for local dev use a tunneling tool — Telegram requires HTTPS)
MINI_APP_URL=https://localhost:3000

# Database (same as server)
DATABASE_URL="mysql://boburov:boburov_wb@localhost:3306/bee_express"

# Redis (shared with server)
REDIS_URL=redis://127.0.0.1:6379
